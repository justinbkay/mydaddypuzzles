<% for story in @stories %>
	<h3><%= story.title %></h3>
	<p>
		<% unless story.picture.nil? %>
			<%= image_tag "/images/articles/#{story.picture}", :style => 'float: left; margin-right: 40px;' %>
			<%= story.body %>
		<% end %>
	</p>
	<br style="clear: both;" />
<% end %>